import os
import json

FS_PATH = ''

TEMPLATE_PATH = os.path.join(FS_PATH, 'webots/maps/template_map.wbt')
TEST_MAP_PATH = os.path.join(FS_PATH,'tests/mapt_test.wbt')

POSSIBLE_OBJECTS_PATH = os.path.join(FS_PATH, 'webots/objects/objects.json')
with open(POSSIBLE_OBJECTS_PATH, 'r') as f:
    POSSIBLE_OBJECTS = json.load(f) 